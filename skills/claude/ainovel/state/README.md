# State directory

This folder is a *sample state area for the skill package*, not the normal location of a user's novel.

Runtime projects are initialized under:

`./novels/<slug>/output/novel/`

The deterministic initializer creates the full runtime tree and copies/creates schema-compatible defaults. Keep this folder only as a structural example for agents inspecting the package.
