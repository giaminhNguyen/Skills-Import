Use the project Ainovel Agent Skill at `.claude/skills/ainovel/SKILL.md`.

Treat `$ARGUMENTS` as the user's `ainovel` command/idea. Load the skill instructions first, resolve the active novel state, and execute the requested workflow using native read/write/bash. Do not configure or call any LLM provider/API.
